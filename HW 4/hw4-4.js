function printArrayElements(array) {
    for (let i = 0; i < array.length; i++) {
        console.log(array[i]);
    }
}

// Використання функції
const sampleArray = ['яблуко', 'банан', 'вишня'];
printArrayElements(sampleArray);
